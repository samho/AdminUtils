#!/usr/bin/env python
#encoding=utf-8

class networking():
    """
    AdminUtils Networking class，用于处理与网络相关的操作，包括远程登录，控制，URL访问等。.	
    """
    def __init__(self, stdin='/dev/null', stdout='/dev/null', stderr='/dev/null'):
	    self.stdin = stdin
	    self.stdout = stdout
	    self.stderr = stderr


	    
    
    